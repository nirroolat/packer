- hosts: all
  become: yes
  tasks:
    - name: Install nginx
      yum: name=nginx
           state=present
    - name: Start nginx on boot
      service: name=nginx
               enabled=yes
               state=started
